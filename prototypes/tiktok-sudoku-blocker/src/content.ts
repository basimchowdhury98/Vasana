// TikTok Sudoku Blocker - Content Script
// This is a stub that will be replaced with the full implementation.

// DOM sentinel so tests can verify the content script injected
// (page.evaluate runs in the main world, but the DOM is shared)
document.documentElement.setAttribute("data-sudoku-blocker", "true");

console.log("[TikTok Sudoku Blocker] Content script loaded.");
