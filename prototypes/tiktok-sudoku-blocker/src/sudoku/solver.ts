import type { SudokuGrid } from "./types.js";

/**
 * Check if placing `num` at (row, col) is valid per Sudoku rules.
 */
export function isValid(
  _grid: SudokuGrid,
  _row: number,
  _col: number,
  _num: number
): boolean {
  // TODO: implement
  throw new Error("Not implemented");
}

/**
 * Solve the given Sudoku grid in place using backtracking.
 * Returns the solved grid or null if unsolvable.
 */
export function solve(_grid: SudokuGrid): SudokuGrid | null {
  // TODO: implement
  throw new Error("Not implemented");
}
